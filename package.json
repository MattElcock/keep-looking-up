{
  "name": "keep-looking-up",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "npx prettier . --write",
    "dev": "concurrently --prefix-colors 'blue,green' -n 'web,mcp' 'npm run dev -w packages/web-ui' 'npm run dev -w packages/observations-mcp'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "3.8.3"
  }
}
