{
  "name": "keep-looking-up",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "npx prettier . --write",
    "dev": "concurrently --prefix-colors 'blue,green,yellow' -n 'web,mcp,conditions' 'npm run dev -w packages/web-ui' 'npm run dev -w packages/observations-mcp' 'npm run dev -w packages/conditions-mcp'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "3.8.3"
  }
}
